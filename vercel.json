{
  "github": {
    "silent": true
  },
  "builds": [
    {
      "use": "@vercel/static-build",
      "src": "package.json",
      "config": {
        "zeroConfig": true,
        "framework": "gridsome",
        "buildCommand": "yarn build",
        "outputDirectory": "dist",
        "rootDirectory": "./"
      }
    }
  ]
}